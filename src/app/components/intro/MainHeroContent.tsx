import { KineticTitle } from "../visual/KineticTitle";

/** Solo texto del hero; la animación Lottie va fuera del carrusel (SpatialCampusExperience). */
export function MainHeroContent() {
  return (
    <div className="text-center md:text-left py-2">
      <KineticTitle />
    </div>
  );
}
